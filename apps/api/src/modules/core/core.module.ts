import { Module } from "@nestjs/common";

// Campaigns, AdSets, Ads, consultas de métricas (metrics_daily).
// Implementação chega a partir da Etapa 4 (integração Meta Ads) e Etapa 7 (dashboard).
@Module({})
export class CoreModule {}
